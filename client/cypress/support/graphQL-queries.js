export const countryNames = `query countryName {
    countries {
        name,
        code,
        capital
    }
}`

export const queryTodos = `query todos {
    getTodos {
        id 
        description
    }
}`